[pytest]
markers =
    whaletag: tests requiring whale tag hardware or simulator (deselect with '-m "not whaletag"')

# By default, exclude whale tag tests unless explicitly requested
addopts = -m "not whaletag"
